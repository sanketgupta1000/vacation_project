package com.project.readers.readers_community.enums;

public enum UserType
{

    // member whose profile is not yet complete
    NEW_MEMBER,
    // member whose profile is completed
    MEMBER,
    ADMIN

}
